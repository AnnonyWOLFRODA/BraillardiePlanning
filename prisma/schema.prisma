datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

generator client {
  provider = "prisma-client-js"
}

model Activity {
  id     String  @id @default(cuid())
  name   String
  author String
}
